#include<stdio.h>           // ไม่มีเครื่องหมาย # // ไม่มีเครื่องหมาย <>
int main() {
    int a = 10;             // ไม่มีเครื่องหมาย ;
    printf("a= %d", a);   // ไม่มีเครื่องหมาย ; 
   
    return 0;               // ไม่มีเครื่องหมาย ;

}